server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: server

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/bacay_online?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:18012005}
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: always

jwt:
  secret: ${JWT_SECRET:bacay_online_super_secret_jwt_key_2026_very_long_and_secure_key_123456789}
  expiration-ms: ${JWT_EXPIRATION_MS:86400000}
